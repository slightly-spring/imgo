spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            # Encryption 필요
            client-id: test
            client-secret: test
            scope: profile, email